test:
  override:
    - py.test --cov=./
  post:
    - bash <(curl -s https://codecov.io/bash)

dependencies:
  pre:
    # - bash envs/make_envs.sh
    - pip install -v scipy
    - pip install pytest-cov
    - apt-get install libpython libpython-dev
  post:
    - python -m nltk.downloader punkt
machine:
  environment:
    PYTHONPATH: "/home/ubuntu/pythia"
  python:
    version: 3.5.1
