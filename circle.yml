machine:
  services:
    - docker
    - mongodb
  environment:
    PYTHIA_MONGO_DB_URI: "localhost:27017"
test:
  override:
    - docker build -t pythia .
  post:
    - bash <(curl -s https://codecov.io/bash)
