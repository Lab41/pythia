test:
  override:
    - py.test --cov=./
  post:
    - bash <(curl -s https://codecov.io/bash)

dependencies:
  pre:
    # - bash envs/make_envs.sh
    - pip install -v scipy
    - pip install pytest-cov
  post:
    - sudo python -m /home/ubuntu/virtualenvs/venv-3.5.1/lib/python3.5/site-packages/nltk.downloader -d /home/ubuntu/virtualenvs/venv-3.5.1/lib/python3.5/site-packages/nltk_data all

machine:
  python:
    version: 3.5.1
