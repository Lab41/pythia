test:
  override:
    - py.test --cov=./
  post:
    - bash <(curl -s https://codecov.io/bash)

dependencies:
  pre:
    - pip install -v scipy
    - pip install pytest-cov

machine:
  python:
    version: 3.5.1
